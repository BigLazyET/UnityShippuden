namespace Assets.Scripts.Enums
{
    public enum EventSourceType
    {
        Unknown,
        BeforeSceneUnload,
        AfterSceneLoad,
        ReloadSlotDisplay,
        AfterReloadSlotDisplay,
        UseItem // 使用道具
    }
}
